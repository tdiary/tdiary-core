{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "tDiary server",
            "type": "Ruby",
            "request": "launch",
            "cwd": "${workspaceRoot}",
            "program": "${workspaceRoot}/bin/tdiary",
            "args": [
                "server"
            ],
            "useBundler": true
        }
    ]
}
